{
  "name": "premium-banking-monorepo",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/admin",
    "apps/api"
  ],
  "scripts": {
    "dev": "concurrently -n api,web,admin -c blue,green,magenta \"npm:dev -w apps/api\" \"npm:dev -w apps/web\" \"npm:dev -w apps/admin\"",
    "build": "npm run build -w apps/api && npm run build -w apps/web && npm run build -w apps/admin",
    "lint": "eslint . --ext .js,.ts,.tsx",
    "format": "prettier --write .",
    "typecheck": "tsc -b",
    "test": "echo 'No tests yet' && exit 0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.5",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  }
}
