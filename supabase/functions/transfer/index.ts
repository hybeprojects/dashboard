// Supabase function removed. Payment/transfer logic should use /api/transactions and Fineract.
console.warn('transfer supabase function removed; use /api/transactions instead');
addEventListener('fetch', () => {});
