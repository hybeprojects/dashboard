// Supabase function removed. Replaced by Next.js API routes under /api/admin.
console.warn('admin-actions supabase function removed; use /api/admin/actions instead');
addEventListener('fetch', () => {});
